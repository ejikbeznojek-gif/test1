Процедура ПриУстановкеНовогоКода(СтандартнаяОбработка, Префикс)
	Префикс=ПолучитьПрефиксНомеров(ЭтотОбъект);
КонецПроцедуры

//Кухарь 30.08.16 начало
Процедура ПередЗаписью(Отказ)
	Если ПараметрыСеанса.ЭтотУзел.ТипУзла=Перечисления.ТипУзлаИБ.Склад тогда
		ЭтоСклад=истина;
	иначе
		ЭтоСклад=Ложь;
	КонецЕсли;
	
	
	
	НадоВыгружать = Ложь;
	Если ОбменДанными.Загрузка Тогда
		НадоВыгружать = Ложь;
	Иначе 
		НадоВыгружать = Истина;
		
		если ЭтоСклад тогда
			если ЭтоНовый() или Состояние=Перечисления.СостоянияКоррекции.Отклонена тогда
				Состояние=Перечисления.СостоянияКоррекции.ВРаботе;
			КонецЕсли;
		иначе
			если ЭтоНовый() тогда
				#Если клиент тогда
					Предупреждение("Создавать чек-листы можно только на складе.");
					Отказ=Истина;
				#Конецесли	
			КонецЕсли;
			
			
		КонецЕсли;
	КонецЕсли;	
		
		
		
		
		
		
		
		
		Если НадоВыгружать Тогда
			УстановитьПолучателейИзмененийОбъекта(ЭтотОбъект);
		КонецЕсли;
		
		
	КонецПроцедуры


	Процедура ОбработкаЗаполнения(Основание)
		// Вставить содержимое обработчика.
	КонецПроцедуры
	//Кухарь 30.08.16 конец