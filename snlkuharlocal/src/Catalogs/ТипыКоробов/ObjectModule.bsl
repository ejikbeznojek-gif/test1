Процедура Печать(Кнопка=Неопределено) Экспорт
	Макет=ПолучитьМакет("Наклейка");	
	
	ОбъектШК			=Макет.Рисунки.ОбъектШК.Объект;
	ОбъектШК.Сообщение	=ШтрихКод;
	
	
	Макет.Параметры.Заполнить(ЭтотОбъект);
	
	ТД=Новый ТабличныйДокумент;
	ТД.Вывести(Макет);
	ТД.ИмяПараметровПечати	="ПараметрыПечати_НаклейкаНаКороб";
	ТД.ОтображатьЗаголовки	=Ложь;
	ТД.ОтображатьСетку		=Ложь;
	
	ТД.Показать();	
КонецПроцедуры

Процедура ПриУстановкеНовогоКода(СтандартнаяОбработка, Префикс)
	Префикс=ПолучитьПрефиксНомеров();
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	//крохин начало 30.01.2013
	Если НЕ ОбменДанными.Загрузка Тогда
	 УстановитьПолучателейИзмененийОбъекта(ЭтотОбъект);	
    КонецЕсли;
    //крохин конец 30.01.2013


КонецПроцедуры
