

Процедура ПередЗаписью(Отказ, Замещение)
	//крохин начало 11.08.2013
	//УстановитьПолучателейИзмененийОбъекта(ЭтотОбъект);
	НадоВыгружать = Ложь;
	СЗ = ЭтотОбъект.ВыгрузитьКолонку("Отработана");
	Для Каждого Элемент Из СЗ Цикл
		Если НЕ Элемент Тогда
		НадоВыгружать = Истина;
		КонецЕсли;
	КонецЦикла;
	Если ОбменДанными.Загрузка Тогда
		НадоВыгружать = Ложь;
		КонецЕсли;
	Если НадоВыгружать Тогда
		УстановитьПолучателейИзмененийОбъекта(ЭтотОбъект);
		КонецЕсли;
	//крохин конец 11.08.2013	
КонецПроцедуры

