
&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	ИмяТипа		= ТекущийОбъект.ОбъектТип;
	МетаОбъект 	= Метаданные.НайтиПоПолномуИмени(ИмяТипа);	
	Если МетаОбъект<>Неопределено Тогда
		ОписаниеТипа   	= ФР_ОбщегоНазначения_ПИ.ОписаниеТипа(ТекущийОбъект.ОбъектТип);		
		Менеджер 		= Вычислить(ОписаниеТипа.КлассМЧ+"["""+ОписаниеТипа.Вид+"""]");
		СсылкаНаОбъект	= Менеджер.ПолучитьСсылку(Новый УникальныйИдентификатор(ТекущийОбъект.ОбъектСсылкаУИД));
	КонецЕсли; 
КонецПроцедуры
