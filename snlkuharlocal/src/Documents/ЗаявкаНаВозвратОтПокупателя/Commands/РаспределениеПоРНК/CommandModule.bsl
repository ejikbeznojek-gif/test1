
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	//Вставить содержимое обработчика.
	//ПараметрыФормы = Новый Структура("", );
	//ОткрытьФорму("Документ.ЗаявкаНаВозвратОтПокупателя.ФормаСписка", ПараметрыФормы, ПараметрыВыполненияКоманды.Источник, ПараметрыВыполненияКоманды.Уникальность, ПараметрыВыполненияКоманды.Окно, ПараметрыВыполненияКоманды.НавигационнаяСсылка);
	
	//Если Элементы.Список.ТекущиеДанные = Неопределено Тогда
	//	Возврат;
	//КонецЕсли;
	//
	//СсылкаНаОбъект = Элементы.Список.ТекущиеДанные.Ссылка;
	//
	//Если ЗначениеНеЗаполнено(СсылкаНаОбъект) Тогда
	//	Возврат;
	//КонецЕсли;
	СсылкаНаОбъект = ПараметрКоманды.Ссылка;
	ФормаОбработки =  Обработки.РаспределениеРКМ_РНК_МЧЗ.ПолучитьФорму("Форма");
	ФормаОбработки.Объект.ДокОбъект = СсылкаНаОбъект;
	ФормаОбработки.Открыть();
	
КонецПроцедуры
