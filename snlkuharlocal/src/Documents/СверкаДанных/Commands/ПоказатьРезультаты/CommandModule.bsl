
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Результаты	= ПолучитьРезультатыСверкиНаСервере(ПараметрКоманды);
	Для Каждого Элемент Из Результаты Цикл
		Элемент.Значение.ТД.Показать();	
	КонецЦикла; 
КонецПроцедуры

&НаСервере
Функция ПолучитьРезультатыСверкиНаСервере(ДокСверки)
	Возврат Документы.СверкаДанных.СравнитьДанныеПоДокументуСверки(ДокСверки);
КонецФункции