   Перем СтароеЗначение;

Процедура ПриЗаписи(Отказ)
	Если СтароеЗначение<>ЭтотОбъект.Значение Тогда
		ЗаписатьВЛог("Константы","Изменение константы : """+ЭтотОбъект.Метаданные().Имя+""". Старое значение: "+СтароеЗначение+", новое значение: "+Значение+", компьютер: "+ИмяКомпьютера(),"Информация");	
	КонецЕсли;	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	СтароеЗначение=Константы[ЭтотОбъект.Метаданные().Имя].Получить();
КонецПроцедуры
